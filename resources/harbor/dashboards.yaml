apiVersion: v1
kind: Dashboard
app: "harbor"
version: 1.0.0
appVersion:
  - latest
configurations:
  - name: Harbor
    kind: Grafana
    image: apache/dashboards/images/Harbor.png
    description: The dashboard provides information on the Harbour instance status, storage usage, projects and tasks.
    file: include/dashboards/Harbor.json