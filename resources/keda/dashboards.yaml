apiVersion: v1
kind: Dashboard
app: "keda"
version: 1.0.0
appVersion:
  - latest
configurations:
  - name: Keda
    kind: Grafana
    image: apache/images/Keda.png
    description: The dashboard provides information on the errors, values of the metrics generated and replicas of the scaled object.
    file: include/Keda.json