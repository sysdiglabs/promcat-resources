apiVersion: v1
kind: Dashboard
app: Fluentd
version: 1.0.0
appVersion:
- '2.9'
configurations:
- name: Fluentd
  kind: Sysdig
  image: fluentd/images/fluentd.png
  description: |
    This dashboard offers information on:
    * Input/Output
    * Buffer
    * Flush
  file: include/Fluentd.json