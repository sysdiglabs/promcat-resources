apiVersion: v1
kind: Dashboard
app: "ntp"
version: 1.0.0
appVersion:
  - latest
configurations:
  - name: ntp
    kind: Grafana
    image: apache/dashboards/images/ntp.png
    description: The dashboard provides information on the drift of each node.
    file: include/dashboards/ntp.json