apiVersion: v1
kind: SetupGuide
app: "haproxy-ingress"
version: 1.0.0
appVersion:
- latest
descriptionFile: INSTALL.md
configurations:
- name: haproxy-ingress-deploy.yaml
  file: include/haproxy-ingress-deploy.yaml