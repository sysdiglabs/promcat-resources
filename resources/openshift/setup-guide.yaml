apiVersion: v1
kind: SetupGuide
app: "openshift"
version: 1.0.0
appVersion:
- latest
descriptionFile: INSTALL.md
configurations:
- name: openshift-state-metrics-deploy.yaml
  file: include/openshift-state-metrics-deploy.yaml