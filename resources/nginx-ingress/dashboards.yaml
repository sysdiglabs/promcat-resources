apiVersion: v1
kind: Dashboard
app: "nginx-ingress"
version: 1.0.0
appVersion:
  - latest
configurations:
  - name: Nginx_Kubernetes_Ingress_Controller
    kind: Grafana
    image: apache/dashboards/images/Nginx_Kubernetes_Ingress_Controller.png
    description: The dashboard provides information on the error rate, resource usage, traffic and certificate expiration of the NGINX ingress.
    file: include/dashboards/Nginx_Kubernetes_Ingress_Controller.json